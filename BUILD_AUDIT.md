# 构建与媒体库兼容性检查（2026-09-14）

用户环境：飞牛影视 **0.9.7-4**；Switch 安装包下载时间为 **9 月 11 日 13:52**。
核验使用 GitHub Actions API、当前 NAS 的公开网页脚本和本地源码；未登录 NAS 读取私人媒体数据。

## 构建对照

| 提交 | GitHub 构建开始时间（北京时间） | 结果 |
| --- | --- | --- |
| `32582f7`（检查开始时的本地 HEAD） | 9 月 11 日 06:31 | 成功；存在未过期的 Switch 产物 |
| `621bd90` | 9 月 11 日 06:22 | 失败 |
| `313a88c` | 9 月 11 日 05:25 | 成功 |

[最新成功构建](https://github.com/z1369810535-sketch/SwitchFnTv/actions/runs/34537852855)的产物名称为
`SwitchFnTv-NintendoSwitch-deko3d`，上传时间为 9 月 11 日 06:37。
用户下载时间晚于该构建，但仅凭下载时间无法确认 SD 卡里的文件就是这个提交。

上述成功状态属于旧源码，以下是本次修复在推送前的检查记录。
新安装包的构建结果以 GitHub Actions 中对应修复提交的运行记录为准。

## 发现与修复

| 位置 | 原有问题 | 本地修改 |
| --- | --- | --- |
| 首页与媒体库入口 | 根据库分类只请求 `TV` 或 `Movie`，会排除目录和未归类视频 | 与当前飞牛首页一致，请求 Movie、TV、Directory、Video |
| 媒体库卡片 | 缺少海报时把库 GUID 当图片路径，且隐藏库名 | 保留可见库名；没有实际海报时显示占位图 |
| 列表解析 | 仅接受部分响应形式；`total` 为字符串或空值会报类型错误 | 接受数组和列表包装；兼容字符串/空总数；未知结构显示错误 |
| 目录导航 | 子目录也使用媒体库的 `ancestor_guid` 查询 | 子目录使用 `parent_guid`，保留直接子项 |
| 分页 | 空页切换查询条件；后续空页可能清空已加载内容；刷新可能被旧响应覆盖 | 保持查询条件，保留已有内容，防止重复加载和旧响应覆盖 |
| 季卡片 | 类型缺失时误认为电影；季编号未正确映射 | 识别季类型、季编号和所属电视剧，按数字排序并去重 |
| 播放列表 | 把电视剧 GUID 直接当作季 GUID 获取分集 | 列举各季并汇总分集，补齐所属剧和季编号；支持没有季容器的分集 |
| 季/集兼容加载 | 季接口备用查询无分页，无法确保完整；分集无备用查询 | 用 `item/list` 分页补齐，检查重复和不完整返回 |
| 卡片复用 | 标题可能沿用旧卡片的隐藏状态 | 重新设置标题、附加文字和进度条显示状态 |
| 错误显示 | 备用接口报错可能掩盖原始错误；季列表错误仅写日志 | 保留原始错误，并在季列表显示加载失败原因 |

当前 NAS 的公开脚本确认：`mediadb/list` 获取媒体库，`season/list/{guid}` 获取季，
`episode/list/{guid}` 获取季内剧集；首页的类型过滤包含 Movie、TV、Directory、Video。
本次修改据此调整浏览路径。是否涵盖用户所有缺失条目，仍需登录后对比实际库和 Switch 显示。

## 验证

使用项目内的便携 Zig C++ 编译器，以 C++17 编译生产文件 `app/src/api/fntv.cpp`。
只替换 HTTP、UI 和配置依赖，十组回归测试全部通过：

1. 媒体库数组、列表包装、字符串状态码及无海报数据。
2. 字符串/空总数、媒体库过滤和子目录查询。
3. 列表结束后不切换查询范围。
4. 缺失季类型、数字季排序、特别篇及季去重。
5. 多季分集汇总和顺序。
6. 205 集跨三页完整加载。
7. 没有季容器的连续剧。
8. 未知返回格式及重复的不完整分页。
9. 无总数、忽略分页参数的服务器。
10. 备用媒体库接口失败后保留原始错误。

测试运行说明见 [tests/README.md](tests/README.md)。已在构建工作流加入回归测试前置任务，
并要求上传阶段必须找到 `.nro` 文件。

本机没有 Switch 工具链；本次验证未覆盖 Switch 链接、界面渲染和实机播放。
生成新包后，应核对库名可见性、未归类视频与目录、末页滚动、每季集数、跨季播放列表及刷新行为。
